# Angie + ModSecurity + OWASP CRS + Certbot + Fail2ban

## 📦 Стек

- **Angie** (`angie-with-modsec`) — веб-сервер с поддержкой `ngx_http_modsecurity_module`.
- **ModSecurity** — веб-фаервол для фильтрации запросов.
- **OWASP CRS** (Core Rule Set) — базовые правила защиты (SQLi, XSS и пр.).
- **Certbot** — автоматическое получение и обновление TLS-сертификатов.
- **Fail2ban** — защита от брутфорса и DoS через анализ логов Angie.

---

## 🚀 Запуск

1. Склонируйте репозиторий и создайте структуру каталогов:

   ```bash
   mkdir -p angie/conf.d angie/stream.d angie/logs modsec certs
